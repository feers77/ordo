[project]
name = "ordo-events"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["ordo-runtime", "ordo-core", "httpx>=0.28", "cryptography>=42", "signxml>=5"]

[tool.uv.sources]
ordo-runtime = { workspace = true }
ordo-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ordo_events"]
